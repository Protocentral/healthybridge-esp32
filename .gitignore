# ESP-IDF build output and generated config (source of truth is sdkconfig.defaults)
build/
sdkconfig
sdkconfig.old
# Component-manager downloads (re-fetched from dependencies.lock, which IS tracked).
managed_components/

# Editor / OS
.vscode/
.DS_Store

# Per-profile build outputs (hp5.sh / hp6.sh)
build.hp5/
build.hp6/
sdkconfig.hp5
sdkconfig.hp6

# Internal working notes: planning, handoffs, bring-up logs. Not part of the
# public repository — docs/ carries the architecture and protocol references.
internal/
